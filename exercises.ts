// Recursion 1
const factorial = (num: number): number => {
  return 0;
};

// Recursion 2
const fibonacci = (num: number): number => {
  return 0;
};

// Recursion 3
const array_sum = (arr: Array<number>): number => {
  return 0;
};

// Recursion 4
const factors = (num: number): Array<number> => {
  return [0];
};

// Currying 1
const curry_multiplication = (num: number): any => {
  return num;
};

// Currying 2
const onclick_curry = (str: string): any => {
  return str;
};
